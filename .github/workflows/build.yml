name: CI

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - name: Build
        run: .\Build\Snow.SnowAutomationPlatform.ServiceNow.Integration.BUILD.ps1
        shell: powershell
